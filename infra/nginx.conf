# ============================================================================
# Brilliant Seas Shipping — Nginx Reverse Proxy
#
# SECURITY: Server block acting as edge gateway
# Handles SSL termination, rate limiting at edge, and CSP headers
# ============================================================================

worker_processes auto;

events {
    worker_connections 1024;
}

http {
    include mime.types;
    default_type application/octet-stream;

    # Basic Settings
    sendfile on;
    tcp_nopush on;
    tcp_nodelay on;
    keepalive_timeout 65;
    types_hash_max_size 2048;
    server_tokens off; # OWASP A05: Do not leak server version

    # SSL Settings (Assume mapped certs)
    ssl_protocols TLSv1.2 TLSv1.3; # Drop TLS 1.0, 1.1
    ssl_prefer_server_ciphers on;
    ssl_ciphers ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384:ECDHE-ECDSA-CHACHA20-POLY1305:ECDHE-RSA-CHACHA20-POLY1305:DHE-RSA-AES128-GCM-SHA256:DHE-RSA-AES256-GCM-SHA384;
    ssl_session_cache shared:SSL:10m;
    ssl_session_timeout 10m;

    # Rate Limiting Zone
    limit_req_zone $binary_remote_addr zone=api_limit:10m rate=10r/s;

    # Backend Upstream
    upstream spring_boot_backend {
        server host.docker.internal:8080;
    }

    server {
        listen 80;
        server_name api.brilliantseas.local;
        
        # Force HTTPS redirect in production
        # return 301 https://$host$request_uri;

        # SECURITY HEADERS
        add_header X-Frame-Options "DENY" always;
        add_header X-Content-Type-Options "nosniff" always;
        add_header X-XSS-Protection "1; mode=block" always;
        # HSTS handled by Spring Boot, but good to double down here:
        add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
        # Content Security Policy (allows UI only from trusted sources)
        add_header Content-Security-Policy "default-src 'self'; script-src 'self'; style-src 'self'; img-src 'self' data:; connect-src 'self' api.brilliantseas.local; frame-ancestors 'none';" always;
        add_header Referrer-Policy "strict-origin-when-cross-origin" always;

        # API Routing
        location /api/ {
            limit_req zone=api_limit burst=20 nodelay;
            
            proxy_pass http://spring_boot_backend;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            
            # Timeout Settings
            proxy_connect_timeout 5s;
            proxy_send_timeout 10s;
            proxy_read_timeout 10s;
        }

        # Block direct access to actuator unless from private IP (simulated)
        location /actuator/ {
            # In real prod: allow 10.0.0.0/8; deny all;
            proxy_pass http://spring_boot_backend/actuator/;
        }
    }
}
